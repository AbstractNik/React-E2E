let hasMovieAdaption= true;
const hasMovie= `${hasMovieAdaption && 'Has Movie'}`
console.log(hasMovie);

let hasMovieAdaptionOR= true;
const hasMovieOr= `${hasMovieAdaptionOR || 'Has Movie'}`
console.log(hasMovieOr);